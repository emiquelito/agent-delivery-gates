# Gate tally

This file records, per rule, every time a gate rejected a deliverable and
what it caught. It is a measurement log, not a design document. Later
tooling in this project builds on top of the entries here.

Each entry says where to see the result, so a reader can check it instead of
taking it. A commit hash was used for that at first and was the wrong choice:
it ties this file to the exact history, and it proves a fix landed, not that
the gate rejected anything.

| # | Date | Rule | Where to see it | What it caught |
|---|------|------|------|----------------|
| 1 | 2026-09-07 | named-spec-files-fail-loud | no test; the build stopped before anything was written | A build step was requested that named a specification file, but that file was not present in the repo. A file with a very similar name was present instead. The gate stopped the build instead of quietly substituting the similar file for the missing one, so nothing got built on a source that was never verified. |
| 2 | 2026-09-07 | full-finding-list | `tests/scan-prose.test.ts` | An independent reviewer checked the prose gate against committed state and reported four ways it passed while checking nothing: a banned word in capitals, a run outside a git repository, a path that did not exist, and a directory given as an argument. Each one exited zero. The reviewer also reported that the gate never said which file had failed. |
| 3 | 2026-09-07 | induced-failure-required | `tests/scan-prose.test.ts` | A fix for the gate was tested only against files made up for the test. Running it against the real repository showed the fix had joined two filenames into one path that could not be opened. The made-up cases had all passed. |
| 4 | 2026-09-07 | full-finding-list | `tests/scan-prose.test.ts` | The second fix asked git for a newline separated file list. git escapes any path holding a non-ASCII character, so a file named with an accent came back as text that could not be opened. The gate stopped with an error blaming the file, and a tracked file holding banned prose went unchecked. A reviewer found it and it was reproduced before any change was made. |
| 5 | 2026-09-07 | full-finding-list | `tests/scan-prose.test.ts` | The file list was matched against the current directory instead of the repository root. Run from the rules directory the gate checked nothing and reported success. Run from the docs directory it checked one file out of seven and reported success. A reviewer found it and it was reproduced from every directory in the repository. |
| 6 | 2026-09-07 | induced-failure-required | `rules/cross-cutting-audit.json` | Widening the gate to cover the rule records caught a banned term on its first run, in the record describing the cross cutting audit. The term had been there since the records were written and no earlier run could have seen it. |
| 7 | 2026-09-08 | commit-before-mutation | no test; the rule itself is `rules/commit-before-mutation.json` | Mutation testing was run against uncommitted work. Restoring each break with a checkout wiped three fixes that had not been committed, exactly as this rule predicts. The fixes were reapplied from the patch text, the work was committed, and mutation testing was run again on committed state, where a checkout is a safe restore. The rule caught its own author. |
| 8 | 2026-09-08 | full-finding-list | `tests/pre-mutation-clean-tree.test.ts` | A reviewer probing the clean-tree hook found three defects the build had not. An unreadable phase file threw and exited 1, which the hook contract treats as non-blocking, so the write would have gone ahead on a dirty tree. An inherited work-tree variable pointed git at another repository, so a dirty tree came back clean and the write was allowed. Reading the input in one call stopped at the pipe buffer, so any payload past about 64KB was rejected as unreadable in every phase, even on a clean tree. |
| 9 | 2026-09-08 | induced-failure-required | `tests/pre-mutation-clean-tree.test.ts` | Breaking the hook two ways failed no tests at all: dropping two of the four guarded tool names, and making one phase block whatever the tree looked like. Both passed a green suite. Six tests were added and each break now fails at least one. |
| 10 | 2026-09-08 | named-spec-files-fail-loud | `tests/scan-prose.test.ts` | The prose gate covered markdown and rule records only, so comments in source were never checked, and two banned words had already reached one. Widening it to source caught them, and produced a false positive on the first run: the spaced-hyphen pattern matched ordinary subtraction. That pattern now applies to prose files and not to source. |
| 11 | 2026-09-08 | full-finding-list | `tests/report-validator.test.ts` | A reviewer probing the report validator found five ways it certified evidence nobody could check. A context phrase wrapped in backticks read as a test command, so the rule written to reject that phrase passed it. Any run of hex letters read as a commit hash, so the word deadbeef proved a claim, and so did a colour code. A prior finding id was matched as a substring, so one id counted as carrying another, and an id left inside a code sample counted as carried forward. Prose calling something a low priority cleanup stood in for a Low finding that was never in the list. |
| 12 | 2026-09-08 | induced-failure-required | `tests/report-validator.test.ts` | The suite passed before and after every one of those five fixes, so nothing in it covered any of them. Twenty-three tests were added. Breaking each fix afterwards failed between one and fourteen tests. |
| 13 | 2026-09-08 | induced-failure-required | no test; recorded here only | A test was committed that pinned a check the code did not yet pass, so the suite was red at that commit. The next commit corrected the check. The tally records the red commit instead of quietly amending it away. |
| 14 | 2026-09-08 | named-spec-files-fail-loud | `tests/scan-prose.test.ts` | The prose gate caught three banned words in comments written while building the validator, including one in a test name. The gate was checking its own author's prose in the same commit that extended it. |
| 15 | 2026-09-08 | full-finding-list | `tests/test-diff-separator.test.ts` | A reviewer found that the check written to catch a weakened test could not see the most ordinary weakening. Swapping a strong assertion for a weaker one, or editing an expected value so the test matches the bug, replaces one line with another, and the count-based rules only notice a line leaving. Commenting an assertion out read as a rewrite for the same reason. Renaming a test file out of the naming rules took it out of every check at once and produced nothing. Go assertions matched no pattern at all. |
| 16 | 2026-09-08 | induced-failure-required | `tests/test-diff-separator.test.ts` | Replacing the per-file count rule with one that merges every test file passed all one hundred and fifty five tests. Nothing proved that a deletion in one test file is not cancelled by an addition in another, which is the behavior the tool depends on. That test now exists and the same break fails eighteen. |
| 17 | 2026-09-08 | commit-before-mutation | no test; recorded here only | A second commit landed with a red suite, because the suite was run after staging instead of before committing. The next commit fixed it. Both stay in history. |
| 18 | 2026-09-08 | named-spec-files-fail-loud | `tests/scan-prose.test.ts` | The prose gate rejected a variable name in new source, not a comment. The name was changed. The gate covers source as well as prose, which is why it fired. |
| 19 | 2026-09-08 | coverage-as-gap-finder | no single test; the sweep is what found it | A sweep of fifty one deliberate changes across the three hooks, the validator, and the prose gate found seventeen that break behavior and fail no test. Read as a gap finder instead of a score, which is what this rule asks for. |
| 20 | 2026-09-08 | induced-failure-required | `tests/scan-prose.test.ts` | Seven of those seventeen were in the prose gate, which runs under its own command and never under the test runner, so nothing in the repository had ever checked it. One of them inverts its exit codes, which makes it report success on prose that breaks the constraints. It had guarded every commit since the first. |
| 21 | 2026-09-08 | induced-failure-required | `tests/pre-mutation-clean-tree.test.ts` | Three separate places read an environment variable that is set but empty as though it were set. One of them turns the clean-tree gate off in any job that exports the phase variable with no value. A signal severity was asserted nowhere, and dropping one alternative from a pattern removed a whole family of assertions from view with every test still passing. |
| 22 | 2026-09-08 | named-spec-files-fail-loud | `tests/scan-prose.test.ts` | Writing tests for the prose gate broke the prose gate. The tests have to contain the words it rejects, and it reads them. The words are built at runtime now. Exempting the gate's own tests was the other option and would have left the gate unable to check the file most likely to describe it. |
| 23 | 2026-09-08 | full-finding-list | `tests/settings-wiring.test.ts` | A reviewer checked the wiring and found that the settings file handed the phase default straight back, so the change making silence mean guarded never engaged for the sessions that use it. Confirmed by running the exact command string the settings file runs: a dirty tree came back allowed. |
| 24 | 2026-09-08 | induced-failure-required | `tests/settings-wiring.test.ts` | With the project directory variable missing, the runtime could not find the hook file and exited 1. A hook contract holds only 0 and 2, so anything else is read as a non-blocking error and the tool call proceeds unchecked. Every command now turns an unexpected failure into a block. |
| 25 | 2026-09-08 | named-spec-files-fail-loud | `tests/scan-prose.test.ts` | The prose gate listed tracked files only, so a new file was never checked until after its first commit. Two banned phrases reached this repo that way, one of them in the commit that added the wiring tests. The list now includes files that are new and not ignored. |
| 26 | 2026-09-08 | cross-cutting-audit | `tests/pre-mutation-clean-tree.test.ts` | The first out-of-band cross-cutting audit, run in a fresh session with no build history, reproduced a live hole in the wired command. With the phase file naming a review and a dirty tree, a session whose working directory sat one level down allowed the write. The phase file was looked for beside the working directory instead of at the repository root. The same class of bug is entry 5, fixed once in the prose gate and never carried across. |
| 27 | 2026-09-08 | cross-cutting-audit | `tests/catalog-code-seam.test.ts` | Two records declared enforcement by a hook that does not exist, one of them the critical filesystem allowlist. The catalog and the hooks also shared no vocabulary, so a finding could not be traced to the rule that produced it. Both sides are tied together now and four tests hold them there. |
| 28 | 2026-09-08 | cross-cutting-audit | `tests/scan-prose.test.ts` | The prose gate covered three kinds of file and left eight tracked files unchecked, config comments and shell scripts among them. It now covers every tracked text file apart from three named exclusions. |
| 29 | 2026-09-08 | cross-cutting-audit | this file | This file, the project's own record of what its gates caught, cited no commit, path, or command across twenty five entries, while the rule it measures requires evidence anyone can check. Every entry now names the commit carrying its fix. |
| 30 | 2026-09-08 | filesystem-allowlist | `tests/path-allowlist.test.ts` | The catalog's only rule marked critical had no enforcing code at all, so the record said prompt. A PreToolUse check now resolves the path a tool is about to touch to where it really points and compares by path segment, so a symlink out of an allowed root is refused and a sibling directory whose name starts the same is not counted as inside. Bash stays uncovered and the record says so. |
| 31 | 2026-09-08 | full-finding-list | `tests/delivery-report-stop-hook.test.ts` | Four copies of the input reader had drifted apart. The same unparseable input blocked in one hook and passed in another, because one copy read a payload it could not parse as an empty one. The rule catching a finding dropped between reports could also only fire from a command line, which is not where an agent is gated. |
| 32 | 2026-09-08 | induced-failure-required | `tests/catalog-code-seam.test.ts` | Two records described mechanical checks finer than the code performs, claiming a hook verifies per-finding fields and confirms the evidence names the induced failure. Neither is done. The records now say what is checked and what is left to a reader. |
| 33 | 2026-09-08 | named-spec-files-fail-loud | `tests/workspace-confinement.test.ts` | The rule keeping the internal working directory out of a published clone had no mechanical check, so it rested on a list run by hand. Three checks now cover it. Writing them broke one of them, since the test file had to name the directory it forbids naming, the same self-reference the prose gate hit. |
| 34 | 2026-09-08 | full-finding-list | `README.md` | A review of the README found it listing a rule under the hooks with its whole obligation, as if a hook checks all of it. The hook checks one part. The records had this same overclaim corrected once already, in entry 32, and the README brought it back in the most visible file in the repo. |
| 35 | 2026-09-08 | induced-failure-required | `README.md` | The README showed both command line tools as bare names. There is no bin entry, so a reader copying either one gets command not found. Every command in that file had been described from the code but not run. |
| 36 | 2026-09-08 | commit-before-mutation | `tests/tally-report.test.ts` | A third commit went in with failing tests. Two tests held a fixed count of tally rows, so adding entries broke them, and the suite was run in the same command as the commit, so the commit ran whatever the result. The tests now count the rows in the file. |
| 37 | 2026-09-08 | filesystem-allowlist | `scripts/pre-publication-check.sh` | The pre-publication check found a home directory path in a test fixture the first time it ran against this repo. The path was made up, not anyone's, but it reads as one and the check cannot tell the difference. The fixture now uses a path that does not look like a home directory. |
| 38 | 2026-09-08 | commit-before-mutation | `.githooks/pre-commit` | Three commits had gone in with failing tests, each because the suite ran in the same command as the commit. A pre-commit hook now runs the typecheck, the tests, the prose scan, and the tally check before a commit is made. Telling myself to check first had not worked, which is the argument this repo makes about prose controls. |
| 39 | 2026-09-08 | induced-failure-required | `tests/precommit-hook.test.ts` | The first version of that hook printed the whole test log on a good commit, tens of thousands of lines. A gate that noisy gets turned off, and a gate nobody runs catches nothing. Output is held back and shown only on a failure, which took a good commit from fifty three thousand characters to seven lines. |
| 40 | 2026-09-08 | named-spec-files-fail-loud | `tests/scan-prose.test.ts` | The pre-commit hook stopped its own arrival. The entry written to record it used a banned word, the prose scan caught it, and the commit did not happen. That was minutes after the hook was turned on and it was the author it stopped. |
| 41 | 2026-09-08 | induced-failure-required | `tests/pre-publication-check.test.ts` | Writing the instructions for the one check a person has to run by hand meant running it the way a person would, which found two faults its own tests had missed. A note to yourself in the names file was searched for as if it were a name. A names file that could not be read exited as a failed check instead of saying it could not run. |
| 42 | 2026-09-08 | full-finding-list | `tests/pre-publication-check.test.ts` | The first real use of the pre-publication check ran it against the placeholder names copied from its own instructions, and it printed PASSED having looked for nothing. A gate reporting success on a check it did not really perform, in the last gate before going public. The instructions handed over a file that was ready to run as written instead of one that would refuse. Names that read as placeholders now make the run incomplete. |
| 43 | 2026-09-08 | induced-failure-required | `tests/scan-prose.test.ts` | Taking the word list out of the prose gate left one case where the pattern for source files came out empty, and grep with an empty pattern matches every line, so every source file failed. Found by checking that a rules file holding only prose rules leaves source alone. A second case swallowed a missing repository into the new nothing-configured path, which would have read as a clean run. |
| 44 | 2026-09-08 | induced-failure-required | `tests/test-diff-separator.test.ts` | Broadening the rules to other languages added a class-name pattern that read Contest.java and Latest.java as test files, because everything was matched without regard to case and case is the only thing that separates those from WidgetTest.java. A tool that calls ordinary source a test file is noisy, and a noisy gate gets switched off. |
| 45 | 2026-09-08 | cross-cutting-audit | `tests/test-diff-separator.test.ts` | The path classifier compiled its own copy of the matching rules, separately from the matcher the separator runs. The moment one of them changed, the two disagreed: the classifier called a file a test while the separator called it source. One place decides now, and a test fails if they ever differ. |
| 46 | 2026-09-08 | full-finding-list | `tests/test-diff-separator.test.ts` | Two of five deliberate breaks failed no test at all: dropping the assertion rule for one Go testing library, and dropping the string form of a Ruby test opener. Writing the missing tests turned up a real fault in the first, where a rule meant to require a capital letter matched lowercase too, so an ordinary module import counted as an assertion. |
| 47 | 2026-09-08 | induced-failure-required | `tests/adg-cli.test.ts` | Packing the tool and installing it somewhere else showed that none of it ran. Node will not strip types from a TypeScript file under node_modules, so every tool and every hook was dead the moment the package was installed, while every test in this repo passed because they run the source in place. Found by installing the tarball instead of describing what would happen. |
| 48 | 2026-09-08 | full-finding-list | `tests/adg-cli.test.ts` | Dropping the templates from the list of files the package ships failed no test, so a published package could have been missing the very files init copies. A test now reads the real package manifest and checks they are in it. |
| 49 | 2026-09-08 | filesystem-allowlist | `tests/init.test.ts` | The hook written into an adopting project had no executable bit, so git ignored it entirely and only printed a hint. The gates looked installed and did nothing, which is the worst outcome available: a gate that is not there but appears to be. |
| 50 | 2026-09-08 | named-spec-files-fail-loud | `tests/tally-report.test.ts` | The tally check looked for a rules directory inside the project being checked. No project adopting this tool has one, so the check failed everywhere except here. Rule ids now come from the installed package when the project has none of its own. |
| 51 | 2026-09-08 | induced-failure-required | `tests/init.test.ts` | The hook called the tool through a command that reaches for the registry when the package is not installed locally, which a one-off run leaves behind. The hook now stops the commit and says how to fix it, because a gate that cannot run is not a gate that passed. |
| 52 | 2026-09-08 | cross-cutting-audit | `src/package-root.ts` | The package root was worked out by counting directories up from a module, which gives a different answer once the code is compiled, because the compiled file sits one level deeper. Two copies of that lookup existed and one of them was wrong, so the rule ids could not be found in any project that installed the tool. One copy now, in one place. |
| 53 | 2026-09-08 | induced-failure-required | `tests/tally.test.ts` | An empty tally was reported as a problem, which meant every project that adopted the gates failed its first commit, because a project that has not caught anything yet has an empty table. That instruction came from this session, and it was wrong for anyone but this repo. |
| 54 | 2026-09-08 | full-finding-list | `tests/scan-prose.test.ts` | A builder reported the tally check failing in a scratch project as unrelated and worked around it by deleting the file. It was neither unrelated nor fixed. Walking the retrofit again turned up two real faults behind it. |
| 55 | 2026-09-08 | induced-failure-required | `tests/cursor-adapter.test.ts` | Cursor treats every exit code other than 0 and 2 as a failure and lets the action through. A crashing hook there fails open by default, which is the opposite of the contract the other platform uses. Every entry now sets failClosed, the adapter never exits 1, and a test drives it with empty, malformed, array and null input to prove it. |
| 56 | 2026-09-08 | cross-cutting-audit | `tests/cursor-adapter.test.ts` | The same gate answers differently on two platforms: an exit code on one, a JSON verdict on the other. Returning one where the other was expected would block nothing and read as success. The contract tests check the denial in each platform's own terms, not ours. |
| 57 | 2026-09-08 | filesystem-allowlist | `tests/path-confinement.test.ts` | Every gate matched on one agent's tool names, so on another agent they would install, match nothing, exit 0 and protect nothing. The path gate was the worst of the three: it let a path outside every allowed root through whenever the tool was called something the list did not hold, and it is the rule marked critical. |
| 58 | 2026-09-08 | cross-cutting-audit | `tests/path-confinement.test.ts` | Two of the three gates were widened for the new agent and the third was left behind, described as out of scope. That left the same fault in the one rule marked critical. All three share one tool-name set now, so they go stale together or stay current together. |
| 59 | 2026-09-08 | induced-failure-required | `tests/copilot-adapter.test.ts` | One platform sends camelCase field names and another sends snake_case. The hooks read the names they knew, got nothing, and allowed everything. The denial field differs too, so a verdict written for one platform means nothing to the other. Both faults look exactly like success from outside. |
| 60 | 2026-09-08 | full-finding-list | `tests/copilot-adapter.test.ts` | Breaking the payload reader to use the wrong field name failed no test, because a missing tool name already errs toward checking and the gate denied for a different reason. The test that catches it drives a tool name that must be allowed, so it only passes when the name is really read. |
| 61 | 2026-09-08 | cross-cutting-audit | `src/agent-adapter.ts` | A second platform meant a second copy of the same decision logic. Two copies of one decision have already drifted twice here, in a path lookup and in tool-name matching. The gates live in one core now and a platform contributes only how to read a payload and how to write a verdict. |
| 62 | 2026-09-08 | full-finding-list | `tests/platform-coverage.test.ts` | A task was reported as finished on the strength of a contract table that covered two of the four platforms, and the claim that a platform could not be forgotten was not backed by any test. Each platform's config was checked only by its own file, so a gate missing from one of them would have been caught by nothing. The user asked whether it was really finished, which is what found it. |
| 63 | 2026-09-08 | induced-failure-required | `README.md` | A first draft of the README said the prose gate had its exit codes inverted and shipped that way. What happened was a deliberate break during a sweep, which no test caught. The gate was never broken. Checking the claim against the entry it came from is what found it, in the one file most likely to be read. |
| 64 | 2026-09-08 | induced-failure-required | `tests/ci-workflow.test.ts` | A workflow checking out a shallow clone leaves the test diff check with no history to read and the prose scan with a partial file list, and both then report success having checked far less than they appear to. Removing one fetch-depth line, or dropping the flag that stops the prose scan passing with no rules configured, each fails a test now. |
| 65 | 2026-09-08 | induced-failure-required | `tests/mcp-server.test.ts` | One line written to standard output that is not a protocol message corrupts the stream, and the client reports a protocol error with no useful cause. A test reads every byte the server writes across a whole session and parses it. Adding a single stray line fails nineteen of twenty tests. |
| 66 | 2026-09-08 | filesystem-allowlist | `tests/mcp-server.test.ts` | A tool that takes a file path and reads whatever it is given is a way to move files out of a machine, wearing the costume of a checker. Paths are confined to the working directory using the same check the path gate uses, and a request for a file outside it comes back as a tool error. |
| 67 | 2026-09-08 | test-diff-reported-apart | `tests/test-diff-separator-cli.test.ts`, `tests/test-diff-separator.test.ts` | A test file renamed so its name no longer matches what a runner collects takes its tests out of the run while leaving them in the repository. Git renders the rename as zero lines changed and the suite reports success with nothing left to fail. The check only noticed a file leaving the test folders. The more likely version, Rust's own `#[cfg(test)]` module living inside an ordinary source file with no rename or folder to notice at all, went unreported at the time. It is now caught when the diff shows a recognised Rust marker on a changed line (`#[cfg(test)]`, `#[test]`, or an assertion macro); a change inside that module whose own lines carry none of those markers still goes unreported, since nothing here parses Rust to find the module boundary. |
| 68 | 2026-09-08 | induced-failure-required | `tests/test-diff-separator-cli.test.ts` | The unit tests for that check used a diff written by hand with rename markers in it, and passed. The tool asks git for the diff without rename detection, so git reports a rename as one file added and another deleted and the check never sees a rename at all. Only a test that renames a file in a real repository catches this. |
| 69 | 2026-09-08 | full-finding-list | `tests/test-diff-separator.test.ts` | An import line counted as an assertion, so deleting a test file reported its import line alongside the real losses. Noise in the one output a person is meant to read closely, in a project that has already recorded twice that a noisy gate gets switched off. |
| 70 | 2026-09-08 | cross-cutting-audit | `tests/plugin-manifest.test.ts` | The plugin layout was written from documentation and shipped with a note saying no real tool had ever loaded it, because none was available here. The user ran the validator: the layout was accepted and one field was missing that seven tests of our own had not asked for. Something outside this session had to run it, which is the same lesson as the out-of-band audit. |
| 71 | 2026-09-08 | cross-cutting-audit | `tests/tool-name-agreement.test.ts` | Two places decided whether a tool call mutates a file: a list of names used by the hooks, and a pattern used by the adapters. They had already drifted, and one tool was guarded on one path and waved through on the other. One function decides now, and a test drives the same names through all three paths and fails if they ever disagree. |
| 72 | 2026-09-08 | induced-failure-required | `src/clean-tree-gate.ts` | Twenty two of twenty eight tool names in that list were invented, written down as guesses and never checked. Two of the agents publish their real names: one sends apply_patch for every file edit whatever its own configuration says, and the other sends Write. The list now holds only names checked against a real agent, and everything else falls to a pattern that says in the code that it is a guess. |
| 73 | 2026-09-08 | induced-failure-required | `tests/test-diff-separator.test.ts`, `tests/test-diff-separator-cli.test.ts` | The Rust module detection shipped with three of its own claims held by no test. A reviewer broke the code five ways and the suite stayed green for three: reading markers on context lines and hunk headings, skipping the wide git re-run when no Rust file is in the diff, and reporting one signal once instead of twice. All three worked, and nothing would have said so if they stopped. This entry replaces the closing sentence of entry 67: a change inside a test module with no marker on its own lines is caught now. |
| 74 | 2026-09-08 | test-diff-reported-apart | `src/test-diff-separator.ts` | A stray closing brace inside a one-line block comment ended a Rust test module early, so a weakened assertion below it went unreported. Deleting only the comment made the same edit report. The design says it should err toward calling something a test, and this did the opposite while the comment describing it said only that the count could be thrown off. |
| 75 | 2026-09-08 | induced-failure-required | `tests/ci-workflow.test.ts` | The shipped CI workflow was changed so the prose scan is skipped unless a project turns it on. Every test for the guard used a stub that exits 0, so appending a shell "or true" to both scan commands left all of them passing. A guard that runs a check and drops its exit code reports a clean build over prose that failed, which is the one outcome the guard exists to prevent. |
| 76 | 2026-09-08 | cross-cutting-audit | `docs/examples/05-adopting-on-an-existing-repository.md` | The retrofit example showed a plain init creating the prose rules and baseline files. It never created them without the preset flag, and the paragraph two lines below already said the gate stays off until a project asks for it. The example contradicted both the tool and itself, in the same commit meant to end that misunderstanding elsewhere. |
| 77 | 2026-09-08 | one-fail-loud-setup-script | `scripts/scan-prose.sh`, `tests/scan-prose.test.ts` | The prose gate had been passing over one of its own source files for as long as that file existed. A NUL byte used as a field separator made grep class the file as binary: grep still exits 0 on a match, but prints "binary file matches" to stderr and nothing to stdout, so the loop counting matches saw none and the file came back clean. Proved by putting a banned word in it and watching the scan report zero matches and exit 0. The scanner now reads every file as text, stops the run if grep ever again claims a match it will not show, and a test walks every tracked file for a NUL byte. |
| 78 | 2026-09-08 | named-spec-files-fail-loud | `scripts/scan-prose.sh`, `tests/scan-prose.test.ts` | Writing a worked example meant quoting what the tools printed, and the prose gate rejected the quote: a spaced hyphen inside a fenced block of real command output matched the rule that stands in for an em dash. Passing it would have meant editing evidence to please a style rule. Typographic rules now stop at a fence while word bans carry on through it, and the first attempt at that fix handed an empty pattern to grep, which matches every line, so the guard kept every match instead of dropping it. |
