# Gate tally

This file records, per rule, every time a gate rejected a deliverable and
what it caught. It is a measurement log, not a design document. Later
tooling in this project builds on top of the entries here.

Every entry names the commit that carries the fix, so a reader can check the
claim instead of taking it. An entry with no such reference would be the kind
of unproven claim this project rejects everywhere else, and for twenty five
entries that is what this file was.

| # | Date | Rule | Fixed in | What it caught |
|---|------|------|------|----------------|
| 1 | 2026-09-07 | named-spec-files-fail-loud | `ac1eed1` | A build step was requested that named a specification file, but that file was not present in the repo. A file with a very similar name was present instead. The gate stopped the build instead of quietly substituting the similar file for the missing one, so nothing got built on a source that was never verified. |
| 2 | 2026-09-07 | full-finding-list | `843f9f9` | An independent reviewer checked the prose gate against committed state and reported four ways it passed while checking nothing: a banned word in capitals, a run outside a git repository, a path that did not exist, and a directory given as an argument. Each one exited zero. The reviewer also reported that the gate never said which file had failed. |
| 3 | 2026-09-07 | induced-failure-required | `3e9869c` | A fix for the gate was tested only against files made up for the test. Running it against the real repository showed the fix had joined two filenames into one path that could not be opened. The made-up cases had all passed. |
| 4 | 2026-09-07 | full-finding-list | `3e9869c` | The second fix asked git for a newline separated file list. git escapes any path holding a non-ASCII character, so a file named with an accent came back as text that could not be opened. The gate stopped with an error blaming the file, and a tracked file holding banned prose went unchecked. A reviewer found it and it was reproduced before any change was made. |
| 5 | 2026-09-07 | full-finding-list | `162e093` | The file list was matched against the current directory instead of the repository root. Run from the rules directory the gate checked nothing and reported success. Run from the docs directory it checked one file out of seven and reported success. A reviewer found it and it was reproduced from every directory in the repository. |
| 6 | 2026-09-07 | induced-failure-required | `ffa9e4c` | Widening the gate to cover the rule records caught a banned term on its first run, in the record describing the cross cutting audit. The term had been there since the records were written and no earlier run could have seen it. |
| 7 | 2026-09-08 | commit-before-mutation | `5bd73fe` | Mutation testing was run against uncommitted work. Restoring each break with a checkout wiped three fixes that had not been committed, exactly as this rule predicts. The fixes were reapplied from the patch text, the work was committed, and mutation testing was run again on committed state, where a checkout is a safe restore. The rule caught its own author. |
| 8 | 2026-09-08 | full-finding-list | `5bd73fe` | A reviewer probing the clean-tree hook found three defects the build had not. An unreadable phase file threw and exited 1, which the hook contract treats as non-blocking, so the write would have gone ahead on a dirty tree. An inherited work-tree variable pointed git at another repository, so a dirty tree came back clean and the write was allowed. Reading the input in one call stopped at the pipe buffer, so any payload past about 64KB was rejected as unreadable in every phase, even on a clean tree. |
| 9 | 2026-09-08 | induced-failure-required | `5bd73fe` | Breaking the hook two ways failed no tests at all: dropping two of the four guarded tool names, and making one phase block whatever the tree looked like. Both passed a green suite. Six tests were added and each break now fails at least one. |
| 10 | 2026-09-08 | named-spec-files-fail-loud | `ade441d` | The prose gate covered markdown and rule records only, so comments in source were never checked, and two banned words had already reached one. Widening it to source caught them, and produced a false positive on the first run: the spaced-hyphen pattern matched ordinary subtraction. That pattern now applies to prose files and not to source. |
| 11 | 2026-09-08 | full-finding-list | `13e5b3f` | A reviewer probing the report validator found five ways it certified evidence nobody could check. A context phrase wrapped in backticks read as a test command, so the rule written to reject that phrase passed it. Any run of hex letters read as a commit hash, so the word deadbeef proved a claim, and so did a colour code. A prior finding id was matched as a substring, so one id counted as carrying another, and an id left inside a code sample counted as carried forward. Prose calling something a low priority cleanup stood in for a Low finding that was never in the list. |
| 12 | 2026-09-08 | induced-failure-required | `13e5b3f` | The suite passed before and after every one of those five fixes, so nothing in it covered any of them. Twenty-three tests were added. Breaking each fix afterwards failed between one and fourteen tests. |
| 13 | 2026-09-08 | induced-failure-required | `9495be6` | A test was committed that pinned a check the code did not yet pass, so the suite was red at that commit. The next commit corrected the check. The tally records the red commit instead of quietly amending it away. |
| 14 | 2026-09-08 | named-spec-files-fail-loud | `13e5b3f` | The prose gate caught three banned words in comments written while building the validator, including one in a test name. The gate was checking its own author's prose in the same commit that extended it. |
| 15 | 2026-09-08 | full-finding-list | `4318070` | A reviewer found that the check written to catch a weakened test could not see the most ordinary weakening. Swapping a strong assertion for a weaker one, or editing an expected value so the test matches the bug, replaces one line with another, and the count-based rules only notice a line leaving. Commenting an assertion out read as a rewrite for the same reason. Renaming a test file out of the naming rules took it out of every check at once and produced nothing. Go assertions matched no pattern at all. |
| 16 | 2026-09-08 | induced-failure-required | `4318070` | Replacing the per-file count rule with one that merges every test file passed all one hundred and fifty five tests. Nothing proved that a deletion in one test file is not cancelled by an addition in another, which is the behavior the tool depends on. That test now exists and the same break fails eighteen. |
| 17 | 2026-09-08 | commit-before-mutation | `a3271a8` | A second commit landed with a red suite, because the suite was run after staging instead of before committing. The next commit fixed it. Both stay in history. |
| 18 | 2026-09-08 | named-spec-files-fail-loud | `4318070` | The prose gate rejected a variable name in new source, not a comment. The name was changed. The gate covers source as well as prose, which is why it fired. |
| 19 | 2026-09-08 | coverage-as-gap-finder | `cb94848` | A sweep of fifty one deliberate changes across the three hooks, the validator, and the prose gate found seventeen that break behavior and fail no test. Read as a gap finder instead of a score, which is what this rule asks for. |
| 20 | 2026-09-08 | induced-failure-required | `d006d05` | Seven of those seventeen were in the prose gate, which runs under its own command and never under the test runner, so nothing in the repository had ever checked it. One of them inverts its exit codes, which makes it report success on prose that breaks the constraints. It had guarded every commit since the first. |
| 21 | 2026-09-08 | induced-failure-required | `cb94848` | Three separate places read an environment variable that is set but empty as though it were set. One of them turns the clean-tree gate off in any job that exports the phase variable with no value. A signal severity was asserted nowhere, and dropping one alternative from a pattern removed a whole family of assertions from view with every test still passing. |
| 22 | 2026-09-08 | named-spec-files-fail-loud | `cb94848` | Writing tests for the prose gate broke the prose gate. The tests have to contain the words it rejects, and it reads them. The words are built at runtime now. Exempting the gate's own tests was the other option and would have left the gate unable to check the file most likely to describe it. |
| 23 | 2026-09-08 | full-finding-list | `8b7bf88` | A reviewer checked the wiring and found that the settings file handed the phase default straight back, so the change making silence mean guarded never engaged for the sessions that use it. Confirmed by running the exact command string the settings file runs: a dirty tree came back allowed. |
| 24 | 2026-09-08 | induced-failure-required | `5151fa2` | With the project directory variable missing, the runtime could not find the hook file and exited 1. A hook contract holds only 0 and 2, so anything else is read as a non-blocking error and the tool call proceeds unchecked. Every command now turns an unexpected failure into a block. |
| 25 | 2026-09-08 | named-spec-files-fail-loud | `5151fa2` | The prose gate listed tracked files only, so a new file was never checked until after its first commit. Two banned phrases reached this repo that way, one of them in the commit that added the wiring tests. The list now includes files that are new and not ignored. |
| 26 | 2026-09-08 | commit-before-mutation | `b599246` | The first out-of-band cross-cutting audit, run in a fresh session with no build history, reproduced a live hole in the wired command. With the phase file naming a review and a dirty tree, a session whose working directory sat one level down allowed the write. The phase file was looked for beside the working directory instead of at the repository root. The same class of bug is entry 5, fixed once in the prose gate and never carried across. |
| 27 | 2026-09-08 | induced-failure-required | `7420720` | Two records declared enforcement by a hook that does not exist, one of them the critical filesystem allowlist. The catalog and the hooks also shared no vocabulary, so a finding could not be traced to the rule that produced it. Both sides are tied together now and four tests hold them there. |
| 28 | 2026-09-08 | full-finding-list | `b599246` | The prose gate covered three kinds of file and left eight tracked files unchecked, config comments and shell scripts among them. It now covers every tracked text file apart from three named exclusions. |
| 29 | 2026-09-08 | induced-failure-required | `1ddb90b` | This file, the project's own record of what its gates caught, cited no commit, path, or command across twenty five entries, while the rule it measures requires evidence anyone can check. Every entry now names the commit carrying its fix. |
