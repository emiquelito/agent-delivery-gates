# Gate tally

This file records, per rule, every time a gate rejected a deliverable and
what it caught. It is a measurement log, not a design document. Later
tooling in this project builds on top of the entries here.

| # | Date | Rule | What it caught |
|---|------|------|----------------|
| 1 | 2026-09-07 | named-spec-files-fail-loud | A build step was requested that named a specification file, but that file was not present in the repo. A file with a very similar name was present instead. The gate stopped the build instead of quietly substituting the similar file for the missing one, so nothing got built on a source that was never verified. |
| 2 | 2026-09-07 | full-finding-list | An independent reviewer checked the prose gate against committed state and reported four ways it passed while checking nothing: a banned word in capitals, a run outside a git repository, a path that did not exist, and a directory given as an argument. Each one exited zero. The reviewer also reported that the gate never said which file had failed. |
| 3 | 2026-09-07 | induced-failure-required | A fix for the gate was tested only against files made up for the test. Running it against the real repository showed the fix had joined two filenames into one path that could not be opened. The made-up cases had all passed. |
| 4 | 2026-09-07 | full-finding-list | The second fix asked git for a newline separated file list. git escapes any path holding a non-ASCII character, so a file named with an accent came back as text that could not be opened. The gate stopped with an error blaming the file, and a tracked file holding banned prose went unchecked. A reviewer found it and it was reproduced before any change was made. |
| 5 | 2026-09-07 | full-finding-list | The file list was matched against the current directory instead of the repository root. Run from the rules directory the gate checked nothing and reported success. Run from the docs directory it checked one file out of seven and reported success. A reviewer found it and it was reproduced from every directory in the repository. |
| 6 | 2026-09-07 | induced-failure-required | Widening the gate to cover the rule records caught a banned term on its first run, in the record describing the cross cutting audit. The term had been there since the records were written and no earlier run could have seen it. |
| 7 | 2026-09-08 | commit-before-mutation | Mutation testing was run against uncommitted work. Restoring each break with a checkout wiped three fixes that had not been committed, exactly as this rule predicts. The fixes were reapplied from the patch text, the work was committed, and mutation testing was run again on committed state, where a checkout is a safe restore. The rule caught its own author. |
| 8 | 2026-09-08 | full-finding-list | A reviewer probing the clean-tree hook found three defects the build had not. An unreadable phase file threw and exited 1, which the hook contract treats as non-blocking, so the write would have gone ahead on a dirty tree. An inherited work-tree variable pointed git at another repository, so a dirty tree came back clean and the write was allowed. Reading the input in one call stopped at the pipe buffer, so any payload past about 64KB was rejected as unreadable in every phase, even on a clean tree. |
| 9 | 2026-09-08 | induced-failure-required | Breaking the hook two ways failed no tests at all: dropping two of the four guarded tool names, and making one phase block whatever the tree looked like. Both passed a green suite. Six tests were added and each break now fails at least one. |
| 10 | 2026-09-08 | named-spec-files-fail-loud | The prose gate covered markdown and rule records only, so comments in source were never checked, and two banned words had already reached one. Widening it to source caught them, and produced a false positive on the first run: the spaced-hyphen pattern matched ordinary subtraction. That pattern now applies to prose files and not to source. |
| 11 | 2026-09-08 | full-finding-list | A reviewer probing the report validator found five ways it certified evidence nobody could check. A context phrase wrapped in backticks read as a test command, so the rule written to reject that phrase passed it. Any run of hex letters read as a commit hash, so the word deadbeef proved a claim, and so did a colour code. A prior finding id was matched as a substring, so one id counted as carrying another, and an id left inside a code sample counted as carried forward. Prose calling something a low priority cleanup stood in for a Low finding that was never in the list. |
| 12 | 2026-09-08 | induced-failure-required | The suite passed before and after every one of those five fixes, so nothing in it covered any of them. Twenty-three tests were added. Breaking each fix afterwards failed between one and fourteen tests. |
| 13 | 2026-09-08 | induced-failure-required | A test was committed that pinned a check the code did not yet pass, so the suite was red at that commit. The next commit corrected the check. The tally records the red commit instead of quietly amending it away. |
| 14 | 2026-09-08 | named-spec-files-fail-loud | The prose gate caught three banned words in comments written while building the validator, including one in a test name. The gate was checking its own author's prose in the same commit that extended it. |
| 15 | 2026-09-08 | full-finding-list | A reviewer found that the check written to catch a weakened test could not see the most ordinary weakening. Swapping a strong assertion for a weaker one, or editing an expected value so the test matches the bug, replaces one line with another, and the count-based rules only notice a line leaving. Commenting an assertion out read as a rewrite for the same reason. Renaming a test file out of the naming rules took it out of every check at once and produced nothing. Go assertions matched no pattern at all. |
| 16 | 2026-09-08 | induced-failure-required | Replacing the per-file count rule with one that merges every test file passed all one hundred and fifty five tests. Nothing proved that a deletion in one test file is not cancelled by an addition in another, which is the behavior the tool depends on. That test now exists and the same break fails eighteen. |
| 17 | 2026-09-08 | commit-before-mutation | A second commit landed with a red suite, because the suite was run after staging instead of before committing. The next commit fixed it. Both stay in history. |
| 18 | 2026-09-08 | named-spec-files-fail-loud | The prose gate rejected a variable name in new source, not a comment. The name was changed. The gate covers source as well as prose, which is why it fired. |
