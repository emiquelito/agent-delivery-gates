{
  "name": "agent-delivery-gates",
  "version": "0.2.1",
  "description": "Delivery gates for AI coding agents, because green tests are not proof. Breaks your code on purpose and reports the breaks no test noticed. Runs a change's new tests against the code from before it. Induces the failure your report says is handled. Git hook, CI step or MCP server. No API key, one sandboxed dependency that cannot open a socket.",
  "license": "Apache-2.0",
  "author": "emiquelito <contact@agentgates.dev>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emiquelito/agent-delivery-gates.git"
  },
  "homepage": "https://github.com/emiquelito/agent-delivery-gates#readme",
  "bugs": {
    "url": "https://github.com/emiquelito/agent-delivery-gates/issues"
  },
  "keywords": [
    "ai-agent",
    "delivery-gate",
    "code-review",
    "git-hooks",
    "cli",
    "prose-lint",
    "test-diff"
  ],
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "bin": {
    "agent-delivery-gates": "dist/bin/adg.js",
    "adg": "dist/bin/adg.js"
  },
  "files": [
    "bin",
    "dist",
    "hooks",
    "scripts",
    "src",
    "presets",
    "rules",
    "templates",
    ".githooks",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "scan": "node hooks/scan-prose.ts",
    "build": "node scripts/build.ts",
    "prepare": "npm run build",
    "prepack": "npm run build",
    "verify": "tsc --noEmit && node --test tests/*.test.ts && node hooks/scan-prose.ts --require-rules && node scripts/tally-report.ts --check && node scripts/refresh-readme-tally.ts --check && node scripts/check-versions.ts --check"
  },
  "dependencies": {
    "web-tree-sitter": "^0.27.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "tree-sitter-c-sharp": "^0.23.5",
    "tree-sitter-go": "^0.25.0",
    "tree-sitter-java": "^0.23.5",
    "tree-sitter-php": "^0.24.2",
    "tree-sitter-python": "^0.25.0",
    "tree-sitter-ruby": "^0.23.1",
    "tree-sitter-rust": "^0.24.0",
    "typescript": "^5.9.3"
  }
}
