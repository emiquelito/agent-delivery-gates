# agent-delivery-gates

Green tests are not proof. This project builds gates that check what an AI
coding agent claims about its own work, not just what it wrote.

Working directory is the git root. Never reach above it.

Commit messages describe the change only.

All tracked prose in this repo follows a set of writing constraints, checked
by `node hooks/scan-prose.ts`. Run it against any markdown file before it is
tracked.
